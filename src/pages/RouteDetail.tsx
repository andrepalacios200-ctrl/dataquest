import{Link,useParams}from'react-router-dom';import{ArrowLeft}from'lucide-react';import{LevelCard}from'../components/ui';import{useContent}from'../context/ContentContext';import{useProgress}from'../context/ProgressContext';export default function RouteDetail(){const{routeId}=useParams();const{routes}=useContent();const route=routes.find(r=>r.id===routeId)||routes[0];const{getRouteProgress}=useProgress();return <div className="mx-auto max-w-6xl px-5 py-8"><Link to="/rutas" className="inline-flex items-center gap-2 text-sm font-bold text-slate-500"><ArrowLeft size={16}/> Volver a rutas</Link><div className="mt-5 rounded-[2rem] border bg-white p-7 shadow-sm"><div className="flex flex-wrap items-center gap-4"><div className="grid h-16 w-16 place-items-center rounded-2xl bg-indigo-50 text-3xl">{route.icon}</div><div><div className="text-xs font-extrabold uppercase tracking-widest text-indigo-600">Ruta</div><h1 className="text-3xl font-black">{route.title}</h1></div></div><p className="mt-5 max-w-3xl text-sm leading-6 text-slate-600">{route.description} <b className="text-slate-900">Los tres niveles están disponibles desde el inicio.</b></p></div><div className="mt-7 grid gap-5 lg:grid-cols-3">{route.levels.map(l=><LevelCard key={l.id} route={route} level={l} progress={getRouteProgress(l.lessons.map(x=>x.id))}/>)}</div></div>}

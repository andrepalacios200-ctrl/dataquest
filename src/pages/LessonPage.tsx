import {useState} from 'react';import{Link,useNavigate,useParams}from'react-router-dom';import{ArrowLeft,CheckCircle2,Code2,Lightbulb}from'lucide-react';import{QuizCard,XPBadge}from'../components/ui';import{useProgress}from'../context/ProgressContext';import{useContent}from'../context/ContentContext';
export default function LessonPage(){const{routeId,levelId,lessonId}=useParams();const{routes}=useContent();const route=routes.find(r=>r.id===routeId)||routes[0];const level=route.levels.find(l=>l.id===levelId)||route.levels[0];const lesson=level.lessons.find(l=>l.id===lessonId)||level.lessons[0];const{isCompleted,completeLesson}=useProgress();const[feedback,setFeedback]=useState('');const[quizFeedback,setQuizFeedback]=useState('');const done=isCompleted(lesson.id);const content=lesson.content!;const next=level.lessons[level.lessons.findIndex(l=>l.id===lesson.id)+1];const navigate=useNavigate();async function finish(){try{const r=await completeLesson(lesson.id,lesson.xp);setFeedback(r.awarded?`¡Buen trabajo! Has ganado ${r.xp} XP y tu progreso quedó sincronizado.`:'Esta lección ya estaba completada. Tu XP no se duplica.')}catch(e:any){setFeedback(e.message||'No se pudo guardar el progreso.')}}return <div className="mx-auto max-w-4xl px-5 py-8"><Link to={`/ruta/${route.id}/${level.id}`} className="inline-flex items-center gap-2 text-sm font-bold text-slate-500"><ArrowLeft size={16}/> Volver al nivel</Link><div className="mt-5 rounded-[2rem] bg-slate-900 p-7 text-white"><div className="flex flex-wrap items-center justify-between gap-3"><span className="rounded-full bg-white/10 px-3 py-1 text-xs font-bold">{lesson.type} · {lesson.duration} min</span><XPBadge xp={lesson.xp}/></div><h1 className="mt-5 text-3xl font-black">{lesson.title}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-white/60">{lesson.description}</p></div><div className="mt-6 grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><div className="space-y-5"><section className="rounded-3xl border bg-white p-6"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-indigo-100 text-indigo-700"><Lightbulb size={19}/></div><h2 className="font-extrabold">Objetivo</h2></div><p className="mt-4 text-sm leading-7 text-slate-600">{content.objective}</p><h3 className="mt-5 font-extrabold">Explicación</h3><p className="mt-2 text-sm leading-7 text-slate-600">{content.explanation}</p></section><section className="rounded-3xl border bg-white p-6"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-cyan-100 text-cyan-700"><Code2 size={19}/></div><h2 className="font-extrabold">{content.exampleTitle}</h2></div><div className="mt-4 rounded-2xl bg-slate-950 p-4 text-sm leading-7 text-cyan-100">{content.example}</div><h3 className="mt-5 font-extrabold">Mini reto práctico</h3><p className="mt-2 text-sm leading-7 text-slate-600">{content.challenge}</p></section></div><div><QuizCard question={content.quiz.question} options={content.quiz.options} correctIndex={content.quiz.correctIndex} explanation={content.quiz.explanation} lessonId={lesson.id} onResult={(ok,bonus)=>setQuizFeedback(ok?`¡Correcto! ${bonus?`+${bonus} XP extra.`:'El bonus ya estaba reclamado.'}`:'No pasa nada, vuelve a intentarlo.')}/>{quizFeedback&&<div className="mt-3 rounded-2xl border bg-white p-4 text-xs font-bold text-slate-700">{quizFeedback}</div>}<div className="mt-4 rounded-3xl border bg-white p-5"><div className="text-xs font-bold text-slate-400">RECOMPENSA</div><div className="mt-2 flex items-center justify-between"><div className="font-extrabold">{done?'Lección completada':'Completar lección'}</div><XPBadge xp={lesson.xp}/></div><button onClick={finish} className={`mt-4 flex w-full items-center justify-center gap-2 rounded-2xl py-3 text-sm font-extrabold text-white ${done?'bg-emerald-600':'bg-indigo-600'}`}><CheckCircle2 size={17}/>{done?'Completada':'Completar lección'}</button>{feedback&&<div className="mt-3 rounded-2xl bg-emerald-50 p-3 text-xs font-bold text-emerald-700">{feedback}</div>}</div>{done&&next&&<button onClick={()=>navigate(`/leccion/${route.id}/${level.id}/${next.id}`)} className="mt-3 w-full rounded-2xl border bg-white py-3 text-sm font-extrabold text-slate-700">Siguiente lección →</button>}</div></div></div>}

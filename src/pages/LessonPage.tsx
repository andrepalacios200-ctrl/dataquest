import{useParams,Link}from'react-router-dom';import{routes}from'../data/content';import{QuizCard,XPBadge}from'../components/ui';import{ArrowLeft,CheckCircle2,Code2,Lightbulb}from'lucide-react';import{useState}from'react';export default function LessonPage(){const{routeId,levelId,lessonId}=useParams();const route=routes.find(r=>r.id===routeId)||routes[0];const level=route.levels.find(l=>l.id===levelId)||route.levels[0];const lesson=level.lessons.find(l=>l.id===lessonId)||level.lessons[0];const[feedback,setFeedback]=useState<string>('');return <div className="mx-auto max-w-4xl px-5 py-8"><Link to={`/ruta/${route.id}/${level.id}`} className="inline-flex items-center gap-2 text-sm font-bold text-slate-500"><ArrowLeft size={16}/> Volver al nivel</Link><div className="mt-5 rounded-[2rem] bg-slate-900 p-7 text-white"><div className="flex flex-wrap items-center justify-between gap-3"><span className="rounded-full bg-white/10 px-3 py-1 text-xs font-bold">{lesson.type} · {lesson.duration} min</span><XPBadge xp={lesson.xp}/></div><h1 className="mt-5 text-3xl font-black">{lesson.title}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-white/60">{lesson.description}</p></div><div className="mt-6 grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><div className="space-y-5"><section className="rounded-3xl border border-slate-200 bg-white p-6"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-indigo-100 text-indigo-700"><Lightbulb size={19}/></div><h2 className="font-extrabold">Aprende en 2 minutos</h2></div><p className="mt-4 text-sm leading-7 text-slate-600">Imagina que tienes una tabla de ventas y quieres responder una pregunta concreta. La habilidad importante no es memorizar una herramienta, sino elegir el paso correcto, comprobar el resultado y explicar qué significa.</p><div className="mt-4 rounded-2xl bg-slate-950 p-4 font-mono text-xs leading-6 text-cyan-200"><span className="text-slate-500">// reto</span><br/>SELECT customer_id, SUM(revenue)<br/>FROM sales<br/>GROUP BY customer_id;</div></section><section className="rounded-3xl border border-slate-200 bg-white p-6"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-emerald-100 text-emerald-700"><Code2 size={19}/></div><h2 className="font-extrabold">Mini reto práctico</h2></div><p className="mt-3 text-sm text-slate-600">Identifica qué parte del ejemplo te permite agrupar los datos por cliente.</p><div className="mt-4 grid gap-2 sm:grid-cols-3">{['SELECT','SUM(revenue)','GROUP BY customer_id'].map((x,i)=><button key={x} onClick={()=>setFeedback(i===2?'¡Exacto! GROUP BY crea los grupos sobre los que agregamos.':'Casi. Prueba otra vez.')} className="rounded-2xl border border-slate-200 p-3 text-left text-xs font-bold hover:border-indigo-400">{x}</button>)}</div>{feedback&&<div className="mt-4 rounded-2xl bg-emerald-50 p-3 text-xs font-bold text-emerald-700">{feedback}</div>}</section></div><div><QuizCard onAnswer={ok=>setFeedback(ok?'¡Buen trabajo! Has ganado 20 XP.':'No pasa nada, vuelve a intentarlo.')}/><div className="mt-4 rounded-3xl border border-slate-200 bg-white p-5"><div className="text-xs font-bold text-slate-400">RECOMPENSA</div><div className="mt-2 flex items-center justify-between"><div className="font-extrabold">Completar lección</div><XPBadge xp={lesson.xp}/></div><button onClick={()=>setFeedback('¡Lección completada! Has sumado XP a tu progreso.')} className="mt-4 flex w-full items-center justify-center gap-2 rounded-2xl bg-indigo-600 py-3 text-sm font-extrabold text-white"><CheckCircle2 size={17}/> Completar lección</button></div></div></div></div>}

import{useState}from'react';import{Flame,Trophy,Zap,BookOpen,RotateCcw}from'lucide-react';import{Stat,AchievementCard}from'../components/ui';import{routes}from'../data/content';import{useProgress}from'../context/ProgressContext';
export default function Profile(){const{xp,streak,completedLessons,resetProgress}=useProgress();const[confirm,setConfirm]=useState(false);const medals=completedLessons.length>=1?1:0;return <div className="mx-auto max-w-6xl px-5 py-8"><div className="rounded-[2rem] bg-slate-900 p-7 text-white"><div className="flex flex-wrap items-center gap-5"><div className="grid h-20 w-20 place-items-center rounded-3xl bg-white/10 text-4xl">👨‍💻</div><div><div className="text-xs font-bold uppercase tracking-widest text-cyan-300">Perfil</div><h1 className="mt-1 text-3xl font-black">Alex Rivera</h1><p className="mt-1 text-sm text-white/50">Aprendiendo Data con práctica diaria</p></div></div></div><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Stat icon={<Zap/>} label="XP total" value={xp.toLocaleString('es-CO')}/><Stat icon={<Flame/>} label="Racha" value={`${streak} días`}/><Stat icon={<BookOpen/>} label="Lecciones" value={String(completedLessons.length)}/><Stat icon={<Trophy/>} label="Medallas" value={String(medals)}/></div><div className="mt-8 grid gap-6 lg:grid-cols-2"><div className="rounded-3xl border border-slate-200 bg-white p-6"><h2 className="font-black">Rutas empezadas</h2><div className="mt-5 space-y-4">{routes.map(r=>{const ids=r.levels.flatMap(l=>l.lessons.map(x=>x.id));const p=Math.round((ids.filter(id=>completedLessons.includes(id)).length/ids.length)*100);return <div key={r.id}><div className="flex justify-between text-sm font-bold"><span>{r.icon} {r.title}</span><span>{p}%</span></div><div className="mt-2 h-2 rounded-full bg-slate-100"><div className="h-full rounded-full bg-indigo-500 transition-all" style={{width:`${p}%`}}/></div></div>})}</div></div><div className="rounded-3xl border border-slate-200 bg-white p-6"><h2 className="font-black">Nivel preferido</h2><p className="mt-2 text-sm text-slate-500">Mid-Senior</p><div className="mt-5 rounded-2xl bg-indigo-50 p-4 text-sm leading-6 text-indigo-900/70">Puedes cambiar de nivel en cualquier momento. La plataforma no bloquea contenido por progreso.</div><div className="mt-6 border-t border-slate-100 pt-5"><h3 className="font-extrabold">Datos locales</h3><p className="mt-1 text-xs text-slate-500">Tu XP, racha, quizzes y lecciones se guardan en este navegador mediante localStorage.</p>{!confirm?<button onClick={()=>setConfirm(true)} className="mt-4 inline-flex items-center gap-2 rounded-xl border border-red-200 px-3 py-2 text-xs font-bold text-red-600"><RotateCcw size={14}/> Reiniciar progreso</button>:<div className="mt-4 rounded-2xl bg-red-50 p-4"><p className="text-xs font-bold text-red-700">¿Seguro? Se borrará el progreso guardado en este navegador.</p><div className="mt-3 flex gap-2"><button onClick={()=>{resetProgress();setConfirm(false)}} className="rounded-xl bg-red-600 px-3 py-2 text-xs font-bold text-white">Sí, reiniciar</button><button onClick={()=>setConfirm(false)} className="rounded-xl bg-white px-3 py-2 text-xs font-bold text-slate-600">Cancelar</button></div></div>}</div></div></div></div>}

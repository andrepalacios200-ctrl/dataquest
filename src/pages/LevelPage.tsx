import{useParams,Link}from'react-router-dom';import{routes}from'../data/content';import{LessonCard,ProgressBar}from'../components/ui';import{ArrowLeft,LockOpen}from'lucide-react';export default function LevelPage(){const{routeId,levelId}=useParams();const route=routes.find(r=>r.id===routeId)||routes[0];const level=route.levels.find(l=>l.id===levelId)||route.levels[0];return <div className="mx-auto max-w-5xl px-5 py-8"><Link to={`/ruta/${route.id}`} className="inline-flex items-center gap-2 text-sm font-bold text-slate-500"><ArrowLeft size={16}/> {route.title}</Link><div className="mt-5 rounded-[2rem] bg-white p-7 shadow-sm border border-slate-200"><div className="flex flex-wrap justify-between gap-5"><div><div className="inline-flex items-center gap-2 text-xs font-extrabold text-emerald-600"><LockOpen size={14}/> NIVEL ABIERTO</div><h1 className="mt-2 text-3xl font-black">{level.title}</h1><p className="mt-2 text-sm text-slate-500">{level.subtitle} · {level.lessons.length} lecciones</p></div><div className="min-w-[180px]"><div className="flex justify-between text-xs font-bold"><span>Progreso</span><span>32%</span></div><div className="mt-2"><ProgressBar value={32}/></div></div></div></div><div className="mt-7 space-y-3">{level.lessons.map((l,i)=><LessonCard key={l.id} lesson={l} routeId={route.id} levelId={level.id} index={i}/>)}</div></div>}

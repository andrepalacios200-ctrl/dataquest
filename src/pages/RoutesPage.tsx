import{RouteCard}from'../components/ui';import{useContent}from'../context/ContentContext';export default function RoutesPage(){const{routes}=useContent();return <div className="mx-auto max-w-7xl px-5 py-8"><div className="rounded-3xl bg-slate-900 p-7 text-white"><div className="text-xs font-extrabold uppercase tracking-widest text-cyan-300">Rutas de aprendizaje</div><h1 className="mt-2 text-3xl font-black">Aprende Data a tu manera.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-white/60">No hay candados. Elige la especialidad y entra directamente al nivel que corresponda a tu experiencia.</p></div><div className="mt-7 grid gap-5 md:grid-cols-3">{routes.map(r=><RouteCard key={r.id} route={r}/>)}</div></div>}
